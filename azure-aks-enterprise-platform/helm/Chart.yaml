apiVersion: v2
name: aks-enterprise-app
description: Helm chart for the AKS Enterprise Platform — packages frontend and backend together.
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: frontend
    version: "0.1.0"
    repository: "file://../frontend"
  - name: backend
    version: "0.1.0"
    repository: "file://../backend"
